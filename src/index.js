import React from 'react';
import ReactDOM from 'react-dom/client';
import Fastmath from './Fasthmath';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <Fastmath />
);
